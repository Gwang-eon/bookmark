{
  "name": "chrome-bookmark-organizer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome bookmark organizer for dead links, duplicates, topics, domains, and importance.",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
